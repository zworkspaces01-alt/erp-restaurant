# DB fix log (append-only; one line per finding: id | fixed/rejected/deferred | what/why)

NOTE 2026-09-12: a previous SEC fixer run was interrupted after inserting a "hardening" section (helper functions + delete guards) before the GRANTS section of the migration; its GRANTS rewrite (revokes for anon / column-level privileges) was NOT applied. Check for existing objects (grep -n current_user_role / is_manager / hardening) before adding, and do not duplicate them.

## 2026-09-12 — group SEC (verified by full test run: 327 PASS, 0 FAIL)
Applied in migration sections `17a. SECURITY HARDENING` and `18. GRANTS`:
- SEC-01 | fixed | Maintained columns (ingredients.current_stock/avg_cost_price, suppliers.current_debt, purchase_orders.total_amount/paid_amount/payment_status/po_number, orders.subtotal/total_amount/total_cogs/order_number/status, order_items.cogs_amount, payroll_periods.status/finalized_at/paid_at/total_net_pay/payment_method) revoked from `authenticated` with column-level INSERT/UPDATE grants on everything else; trigger fns + mutating RPCs are SECURITY DEFINER (40 of 48 public functions). Verified: authenticated UPDATE on those columns raises insufficient_privilege while normal columns stay writable.
- SEC-02, SEC-07, SEC-08 | fixed | BEFORE DELETE guards: payroll_periods (draft only -> PAYROLL_PERIOD_LOCKED), supplier_payments (manager/owner), purchase_orders with paid_amount>0 (manager/owner), employees with timekeeping (manager/owner). Helpers `current_user_role()` / `is_manager()`. New RPC `reopen_payroll(uuid)` (finalized -> draft; paid stays paid).
- SEC-03 | fixed | TRUNCATE/TRIGGER/REFERENCES revoked from anon + authenticated.
- SEC-04, SEC-05 | fixed | anon and PUBLIC hold nothing in schema public (tables, sequences, functions + default privileges). RPC EXECUTE granted to authenticated + service_role only; trg_* and handle_new_user get no EXECUTE.
- SEC-06 | fixed | profiles.role not updatable by the owner of the row (column-level revoke).
- SEC-10 | fixed | app_settings validation trigger (timezone must exist in pg_timezone_names, allow_negative_stock boolean, food_cost_target_pct 0..100) -> INVALID_SETTING.
- SEC-12 | fixed | created_by stamped from auth.uid() (client value ignored).
- Tests: supabase/tests/05_rls.sql updated — anon is now denied at the privilege level (no longer "0 rows"), fixture captures the menu item id before switching role, ingredient count assertion made robust.
- SEC-09, SEC-11, SEC-13, SEC-14, SEC-15, SEC-16 | open | handed to the follow-up run.

2026-09-12 (phiên kế hoạch):
- Xác nhận đợt hardening ĐÃ hoàn tất trong migration: mục 17a (helper role, guard xóa payroll/payment/PO/employee, validate app_settings, stamp created_by, chuyển trigger+RPC sang SECURITY DEFINER) và mục 18 GRANTS (revoke anon/public, bỏ TRUNCATE/TRIGGER/REFERENCES, column-level privileges cho cột MAINTAINED). Ghi chú "GRANTS chưa áp" ở đầu file này đã lỗi thời.
- TEST-01 | fixed | `supabase/tests/05_rls.sql` đỏ ở `auth_uid_from_claims` trên `db:test:real`: image supabase/postgres trần ship auth.uid() kiểu cũ chỉ đọc `request.jwt.claim.sub`, còn test set `request.jwt.claims`. Test nay set CẢ HAI dạng claim → `db:test` và `db:test:real` đều xanh.
- Sinh lại src/types/database.ts và docs/DATABASE.generated.md theo migration hiện tại (thêm reopen_payroll, current_user_role, is_manager).
- Findings còn lại (T-01 phân bổ thanh toán, BL-04/T-06 kỳ lương chồng ngày, SEC-09/T-10 check tiền lương, BL-01 WAC khi xóa dòng PO, BL-02 nhân viên nghỉ giữa kỳ, T-09/BL-07 múi giờ, DOC-07 hao hụt lùi ngày, SEC-13 index, SEC-15/T-16, SEC-16 seed guard, DOC-04 khấu hao) → xếp vào Phase 2 của docs/PLAN.md.

## 2026-09-12 — group T (triggers & transactions)
- T-09 | fixed | D8: new stable helper `public.local_today()` (= to_local_date(now())); every `current_date` in the migration (16 sites) replaced — defaults of purchase_orders.order_date, supplier_payments.payment_date, expense_records.expense_date, new default on timekeeping.work_date, next_po_number/next_order_number arg defaults, trg_purchase_orders_before, create_purchase_order/record_supplier_payment, overdue in v_supplier_debt_summary / v_purchase_orders_summary / get_dashboard_stats.
- T-02 | fixed | D10: trg_po_items_after_insert / _after_delete now `select ... for update` the parent purchase_orders row (same lock order in both) BEFORE recomputing total_amount from the line aggregate, so concurrent line inserts can no longer drop each other from the total.
- T-01 | fixed | D10: trg_spa_apply INSERT branch locks the payment + PO rows and raises PAYMENT_NOT_FOUND / PO_NOT_FOUND / PO_SUPPLIER_MISMATCH / PAYMENT_EXCEEDS_PO_DEBT / ALLOCATION_EXCEEDS_PAYMENT (sum of the payment's allocations > payment.amount). DELETE branch raises ALLOCATION_DELETE_NOT_ALLOWED unless the parent payment is already gone (cascade).
- T-11 | fixed | D9: trg_order_items_after_insert raises ORDER_CANCELLED when the parent order is cancelled; trg_orders_before raises ORDER_DATE_LOCKED when order_date moves and the caller is not is_manager() (order_number and the 'sale' ledger rows keep the original date). discount >= 0 CHECK was already present.
- T-03 (discount part) | fixed | D9: trg_orders_before raises DISCOUNT_EXCEEDS_SUBTOTAL when the discount itself changes and exceeds subtotal (not on the partial subtotals create_order writes while inserting lines - the RPC re-checks at the end). Direct writes to subtotal/total_amount/total_cogs/status are already blocked by the SEC-01 column privileges.
- T-12 | fixed | D14: trg_purchase_orders_before (UPDATE) raises PO_SUPPLIER_LOCKED when supplier_id changes while supplier_payments/allocations reference the PO.
- BL-10 (T-side) | fixed | D10: same trigger recomputes due_date = order_date + supplier.payment_terms_days when order_date changes and due_date was not set in the same UPDATE.
- T-16 | fixed | D14: trg_expense_records_before raises EXPENSE_INVALID when status='paid' with payment_method null or amount <= 0. supabase/tests/04_hr_expenses_pnl.sql fixture updated (two 'paid' rows now pass payment_method 'cash'); no assertion changed.
- T-13 | fixed | D14: record_stock_adjustment returns null without writing a ledger row when a stocktake count equals the book stock (v_delta = 0).
- T-08 | fixed | D7: get_pnl_report cogs_waste = -sum(quantity * unit_cost) over ledger rows with txn_type in ('waste','adjustment','stocktake') in range - sign aware, so shortages cost and surpluses credit; 'sale'/'sale_reversal'/'purchase' stay out. Tests updated (fixture has waste -10, stocktake -5, stocktake +5 @1000): pnl_cogs_waste 15000 -> 10000 and the derived pnl_cogs_total/gross_profit/gross_margin_pct/net_profit/net_margin_pct/pnl_monthly_jan_net.
- T-05 | fixed | trg_payroll_periods_before_update now also freezes name/period_start/period_end/payment_method once status='paid' (PAYROLL_PERIOD_PAID). The DELETE guard was already added by SEC-02.
- T-07 | fixed | trg_payroll_periods_before_update raises PAYROLL_NO_ITEMS on draft->finalized with no items and PAYROLL_PAYMENT_METHOD_REQUIRED on finalized->paid without a payment_method, so a direct status UPDATE cannot bypass finalize_payroll/pay_payroll. (Marking items is_paid stays in pay_payroll: doing it from the period BEFORE trigger would re-enter the items->period total sync.)
- T-04 | fixed | trg_inventory_txn_before_insert raises LEDGER_MANUAL_FORBIDDEN for txn_type in ('purchase','sale','sale_reversal') unless the transaction-local flag `app.ledger_internal` is on; the flag is set (and cleared) around the four internal ledger inserts (PO item insert/delete, order-item sale, cancel_order reversal). Manual corrections keep going through record_stock_adjustment (waste/adjustment/stocktake).
- BL-01 (T-side) | fixed (behaviour deferred) | D10: PO-item DELETE is now manager/owner only (PO_ITEM_DELETE_FORBIDDEN). The avg cost is still NOT recomputed when a PO line is deleted - that would need the whole purchase history; documented as a limitation in docs/DATABASE.md.
- T-06, T-10, T-15 | deferred (handed to the BL/payroll fixer) | These are the D5 payroll items (btree_gist EXCLUDE on payroll_periods / PAYROLL_PERIOD_OVERLAP, NET_PAY_NEGATIVE BEFORE trigger, FT with zero timekeeping = full base+allowance). The T group was told to leave D5/D6 payroll math to the run after it; no trigger change was made for them.
- T-14 | deferred | pg_advisory_xact_lock in next_order_number/next_po_number is correct and cheap; replacing it with a per-day counter row has the same blocking semantics (a row lock instead of an advisory lock) and would add a table + a new failure mode. Numbering stays correct; the mitigation is short transactions (create_order / create_purchase_order already are) — documented in docs/DATABASE.md §5.12.
- T-13 (second half) | deferred | recipe qty_out rounding to 0.000 for very small per-portion quantities: fixing it means widening inventory_transactions.quantity beyond numeric(14,3) or accumulating cost before rounding, which changes the ledger's storage contract and every stock assertion. Not worth the regression risk in this pass.
- DOC | docs/DATABASE.md updated: §4.1-4.9 (PO lock + manager-only line delete + WAC limitation, PO_SUPPLIER_LOCKED + due_date recompute, allocation validation, manual-ledger guard, ORDER_CANCELLED/ORDER_DATE_LOCKED/DISCOUNT_EXCEEDS_SUBTOTAL, payroll period guards, EXPENSE_INVALID), §5.5 (stocktake no-op + adjustment in cogs_waste), §5.9 (sign-aware cogs_waste), §5.12 (local_today / timezone rule), §6 views, §8 new error codes with Vietnamese messages.
- GROUP T VERIFIED | full `bash scripts/db-test.sh fx_trg` prints OK with 327 PASS / 0 FAIL; docs/DATABASE.generated.md regenerated (local_today + new defaults). Manual checks: PO_SUPPLIER_LOCKED, ALLOCATION_EXCEEDS_PAYMENT, ALLOCATION_DELETE_NOT_ALLOWED, ORDER_CANCELLED, DISCOUNT_EXCEEDS_SUBTOTAL, EXPENSE_INVALID, PAYROLL_NO_ITEMS, LEDGER_MANUAL_FORBIDDEN all raise; stocktake == book stock returns null with no ledger row; due_date follows a re-dated PO.

## 2026-09-12 — group BL (business logic, D5/D6/D7/D8/D13 + BL parts of D10/D14)
Note: an earlier interrupted BL run had already written part of D5/D6 into the migration (btree_gist + EXCLUDE, no-overlap trigger, gross_pay, generate_payroll overlap filter, NET_PAY_NEGATIVE, SEC-09 checks) but left the test suite RED (pr_item_rows 3 -> 6, then PAYROLL_PERIOD_OVERLAP). This run verified/completed that work and made the suite green again.
- BL-02 | fixed | D5: generate_payroll selects employees by employment overlap ((start_date is null or start_date <= period_end) and (end_date is null or end_date >= period_start)); is_active is NOT part of the predicate, so a leaver is still paid for the period they worked. The "no longer eligible" cleanup uses the same overlap predicate. Test fixture 04 now retires the seed employees with end_date '2024-12-31' instead of is_active=false (that is what isolates the fixture now) — assertion values unchanged.
- BL-04 / T-06 | fixed | D5: `create extension btree_gist` + `exclude using gist (daterange(period_start, period_end, '[]') with &&)` on payroll_periods, plus BEFORE INSERT/UPDATE trigger trg_payroll_periods_no_overlap raising PAYROLL_PERIOD_OVERLAP so the app gets a code instead of SQLSTATE 23P01. New assertion pr_overlapping_period_raises.
- T-15 / SEC-09 | fixed | D5: net_pay < 0 raises NET_PAY_NEGATIVE from the payroll_items BEFORE trigger (not a CHECK); payroll_items money/time columns have >= 0 CHECKs.
- T-10 | fixed | D5: a full-time employee with ZERO timekeeping rows in the period is paid the full base_salary + allowance (attendance not tracked); with rows -> prorated by distinct days / standard_days_per_month, capped at 1.0. New assertion pr_ft_no_timekeeping_full_pay.
- BL-14 | fixed | D5: allowance now applies to BOTH employment types — FT prorated like base pay, PT paid in full (was silently discarded).
- BL-03 | fixed | D6: new generated column payroll_items.gross_pay = base_pay+allowance+bonus+tips-penalty (advance_deduction is cash timing, not cost).
- BL-06 | fixed | D6: get_pnl_report.labor_cost = sum(gross_pay) over payroll periods of ANY status whose period_end falls in the range (draft = month-to-date estimate), so an open month no longer shows zero labor; get_dashboard_stats.month_labor_cost inherits it. Tests updated: Jan labor 18,055,000 (net, finalized only) -> 18,085,000 (gross) and the derived pnl_net_profit/pnl_net_margin_pct/pnl_monthly_jan_net; the old "draft period excluded" case became pr_draft_apr_rows / pr_draft_period_total / pnl_monthly_apr_labor_draft (draft 04/2025 counts 24,800,000) and pnl_monthly_rest_empty now skips month 4.
- BL-09 | fixed | D13: v_menu_engineering popularity threshold N = count of ACTIVE menu items (the whole menu, Kasavana-Smith), not only the items that sold. Test me_threshold_0175 -> me_threshold_014 (0.7/5 active items instead of 0.7/4 sold); no class changed.
- BL-08 | fixed | D13: menu_class is now derived from the same two booleans as is_popular/is_profitable — no coalesce(benchmark_cm, 0) / coalesce(threshold, 1). With no sales in the window benchmark_cm is null, nothing is profitable and every item is 'dog' (no data), instead of silently becoming 'puzzle'. New assertion me_class_matches_flags.
- SEC-15 | fixed | D14: expense_categories gets updated_at + trg_expense_categories_updated_at.
- BL-11 | fixed | record_stock_adjustment gained `p_txn_at timestamptz default null` (null -> now(), future dates raise INVALID_TXN_DATE) written to inventory_transactions.created_at, so waste/stocktake entered after month-end lands in the month it happened (the seed had to bypass the RPC for exactly this). stock_after stays the balance at write time - documented. New assertions adj_backdated_created_at / adj_backdated_lands_in_dec / adj_future_date_raises.
- BL-05, BL-07, BL-10, BL-12 | fixed by the T run (D9/D8/D10/D7) | verified in this run: trg_orders_before raises DISCOUNT_EXCEEDS_SUBTOTAL/ORDER_DATE_LOCKED, every current_date is public.local_today(), due_date follows a re-dated PO, cogs_waste is sign aware over waste/adjustment/stocktake. No further change needed.
- BL-01 | deferred (unchanged from the T run) | Un-blending the weighted average on PO-line DELETE needs the purchase history the schema does not keep (or a prev_avg/prev_stock snapshot per line, only valid when the line is the last movement). Mitigation in place: PO-item DELETE is manager/owner only (PO_ITEM_DELETE_FORBIDDEN); limitation documented in docs/DATABASE.md §4.1.
- BL-13 | deferred | Replacing the transaction-local GUCs (app.payroll_unlock / app.order_items_unlock / app.ledger_internal) with a current_user/owner check would make the guards a no-op for the owner and the service_role - and every RPC is now SECURITY DEFINER owned by postgres, so the guard would stop protecting anything (the 'edit a finalized period raises PAYROLL_PERIOD_LOCKED' assertions run as postgres). The real barrier is the SEC-01 column privileges + the immutability triggers; the GUC only widens a window inside one transaction of an already-authorized RPC. Left as is.
- DOC | docs/DATABASE.md updated: §1.3 + §3 (payroll_items gross_pay, expense_categories updated_at), §4.8 (NET_PAY_NEGATIVE + >=0 checks + the no-overlap EXCLUDE/PAYROLL_PERIOD_OVERLAP), §5.5 (p_txn_at, business-time dating), §5.6 (overlap-based eligibility, FT-with-no-timekeeping = full pay, allowance for both types), §5.9 (labor_cost = gross_pay, any status, draft = estimate), §6 v_menu_engineering (N = active menu items, class from the two flags, benchmark null = 'dog'/empty state), §8 (PAYROLL_PERIOD_OVERLAP, NET_PAY_NEGATIVE, INVALID_TXN_DATE with Vietnamese messages). docs/DATABASE.generated.md regenerated from fx_bl.
- GROUP BL VERIFIED | full `bash scripts/db-test.sh fx_bl` prints OK with 335 PASS / 0 FAIL (327 before this run: +8 new assertions, 6 existing ones updated for D5/D6/D13). src/types/database.ts NOT regenerated - `npm run db:types` runs against the `postgres` database, which only the Final stage may touch (new/changed signatures there: payroll_items.gross_pay, record_stock_adjustment.p_txn_at, expense_categories.updated_at).

## 2026-09-12 — group SEC (leftovers: SEC-09/11/13/14/15/16)
- SEC-09 | fixed | D14: new migration section `17b. SANITY CONSTRAINTS` (idempotent DO block) adds 11 CHECKs — ingredients (avg_cost_price>=0, min_alert_stock>=0), inventory_transactions (quantity<>0, unit_cost is null or >=0), purchase_orders (due_date>=order_date, total_amount>=0, paid_amount>=0), employees (end_date>=start_date), orders (subtotal>=0, total_cogs>=0), order_items (cogs_amount>=0). payroll_items' eight >=0 checks were already added by the BL run. NOT added, on purpose: `orders.discount <= subtotal` and `orders.total_amount >= 0` (create_order inserts the order with the discount while subtotal is still 0 and then builds the subtotal line by line — the intermediate row would violate them; trg_orders_before raises DISCOUNT_EXCEEDS_SUBTOTAL instead), `purchase_orders.paid_amount <= total_amount` (deleting a paid PO line legitimately drops total_amount below paid_amount — test po2_delete_*), `ingredients.current_stock >= 0` (negative stock is a supported mode). Test fixture updated: supabase/tests/04_hr_expenses_pnl.sql retired the seed employees with end_date '2024-12-31' while their start_date was 2025-01-20 → now sets start_date '2024-01-01' too (same intent: employment entirely before the test window); no assertion value changed.
- SEC-11 | fixed | The `app.*` unlock GUCs are placeholders any role can set, and the guards run inside SECURITY DEFINER triggers so `current_user` is always postgres. New helper `public.internal_unlocked(text)` = GUC 'on' **and** `pg_trigger_depth() > 1`: every legitimate internal write (PO-item→ledger, order-item→ledger+cogs_amount, cancel→sale_reversal, order-line cogs update) is nested inside another trigger (depth>=2), while a hand-written statement from an authenticated session runs at depth 1 → setting the GUC opens nothing. `app.payroll_unlock` removed entirely: pay_payroll only stamps is_paid/paid_at, so trg_payroll_items_guard now allows an UPDATE on a non-draft period only when `to_jsonb(new)-'is_paid'-'paid_at'-'updated_at'-'net_pay'-'gross_pay'` is unchanged (net_pay/gross_pay are GENERATED → null in a BEFORE trigger). Session-role based variants were rejected: PostgREST's session_user is `authenticator`, so they would have broken the real app while tests (postgres) stayed green.
- SEC-13 | fixed | Added `idx_purchase_orders_po_number_pattern on purchase_orders (po_number text_pattern_ops)` and `idx_orders_number_pattern on orders (order_number text_pattern_ops)`; under en_US.UTF-8 the unique btree cannot serve the `like 'PO-yyyymmdd-%'` scan in next_po_number/next_order_number, which ran a full scan while holding the per-day advisory lock.
- SEC-14 | fixed (policy part) / deferred (role-aware policies) | `for all to authenticated using (true) with check (true)` on all 18 tables + `profiles select ... using (true)`: `to authenticated` already is the role filter, and the duplicated `auth.role() = 'authenticated'` was evaluated per row (not hoisted) and is deprecated. The owner/manager/staff RLS matrix is NOT added: with every RPC now SECURITY DEFINER owned by postgres, RLS does not apply inside them, so a policy-level role split would be cosmetic until the RPCs themselves check `is_manager()` — the helpers (`current_user_role()`, `is_manager()`) already exist and gate the delete guards; documented in §1.5 as an app-level responsibility.
- SEC-15 | fixed (verified, done by the BL run) | expense_categories.updated_at + trg_expense_categories_updated_at present; column verified on a fresh database.
- SEC-16 | fixed | supabase/seed.sql now opens with a guard raising `SEED_BLOCKED` unless the session opted in (`set app.allow_seed = 'on'`) or `auth.users` holds no account other than the demo one, so `supabase db reset --linked` / a paste into a real project's SQL editor cannot truncate production and plant admin@restaurant.local. scripts/db-test.sh pipes the opt-in ahead of the seed. Verified manually: with a foreign auth user present the seed aborts and the data survives. The "rotate the demo password" warning is in DATABASE.md §1.5.
- Tests | supabase/tests/05_rls.sql section 3b (runs as the owner, after `reset role`): sec09_sanity_checks_present, sec09_employee_end_before_start, sec09_ledger_zero_quantity, sec11_guc_cannot_unlock_ledger, sec11_guc_cannot_unlock_order_items, sec11_paid_period_items_locked.
- DOC | docs/DATABASE.md §1.5 (RLS policy simplification, unforgeable unlock flags, seed guard + password rotation), new §1.6 (the CHECK table + what was deliberately left to triggers), §4.8 (payroll items: is_paid/paid_at is the only exception), §5.12 (text_pattern_ops indexes), §8 (SEED_BLOCKED + a note that CHECK violations arrive as SQLSTATE 23514 with the constraint name, no CODE prefix). docs/DATABASE.generated.md regenerated from fx_secr.
- SEC VERIFIED | full `bash scripts/db-test.sh fx_secr` prints OK with 341 PASS / 0 FAIL (335 before this run: +6 new assertions, 0 changed).

## 2026-09-12 — group DOC (documentation reconciliation)
- DOC-01 | fixed | docs §5.6 + §2 (`employment_type.full_time`) now state the D5 rule the SQL implements: FT with timekeeping rows = prorated by distinct days / standard_days_per_month (capped 1.0), FT with **no** rows in the period = full base_salary, allowance paid to both types. The spec's "lương cứng/tháng" is met; the deviation the finding described (FT with no chấm công → 0) no longer exists.
- DOC-02 | fixed (doc) | §4.1 documents both deviations from the spec's unconditional WAC formula: `old_stock <= 0` → `avg = cost_base` (a blended average over a zero/negative balance is meaningless) and PO-line DELETE does not un-blend `avg_cost_price` (BL-01 limitation, manager-only delete as mitigation).
- DOC-03 | fixed (doc) | §4.2 states that at INSERT the PO total is 0, so `suppliers.current_debt` actually grows through the UPDATE path as lines are inserted; the invariant `current_debt = Σ debt_amount` holds either way. Spec trigger #1 is satisfied in effect.
- DOC-04 | fixed (doc) | §5.9: `net_profit` is net profit, **not** EBITDA — every `expense_records` row in range is subtracted, depreciation included; the add-back is an app-side category filter (keep "Khấu hao" as its own category). No SQL change: an EBITDA flag is a reporting feature, not a defect.
- DOC-05 | fixed (doc) | The SQL changed under this finding (BL-06/D6): labor now counts periods of ANY status. Stale line in §5.11 (`month_labor_cost` stays 0 until finalized) rewritten; §2 `payroll_status` rows updated (draft = month-to-date estimate, finalized = locked/reopenable).
- DOC-06 | fixed (doc) | §6 `v_menu_engineering`: the coalesce fallbacks are gone (BL-08) and the section now also states that popularity is measured in **units sold**, not revenue — the spec's "doanh số" is read as Kasavana-Smith's quantity rule; profitability is per-unit CM vs the weighted-average CM.
- DOC-07 | fixed (doc) | `record_stock_adjustment` gained `p_txn_at` (BL-11). §5.5 header corrected to the real default (`= null`, null → now()) and documents business-time dating + `stock_after` staying the balance at write time.
- DOC-08 | fixed (doc) | The cascade hole is closed by `trg_payroll_periods_before_delete` (SEC-02). §3 delete recap rewritten (was "cascades items (any status)") and §4.8 gained the DELETE guard paragraph: the payment trail of a finalized/paid period can no longer be erased by deleting the parent.
- DOC-09 | fixed (SQL + doc) | The migration's own comment claimed "trigger functions (trg_*, handle_new_user) get no EXECUTE", but Supabase's `alter default privileges ... grant all on functions to authenticated` (scripts/db-shim.sql, and the real project) had already granted EXECUTE at creation time, so excluding them from the grant loop was a no-op — 29 trigger functions were callable by `authenticated`. Added an explicit `revoke all on function ... from authenticated, anon, public` loop for `trg_*` / `handle_new_user` / `set_updated_at` in section 18 (trigger firing is unaffected: EXECUTE on a trigger function is checked at CREATE TRIGGER time). Verified: 0 of 29 now executable by authenticated, `anon=false` on every function. Documented in §1.5.
- DOC-10 | fixed (doc) | §1.5 already records SEC-14's decision; §2 `user_role` row now says exactly what the enum does control (`is_manager()` → delete guards, `reopen_payroll`, re-dating an order, deleting a PO line) and that **no RLS policy** references it — role separation is an app-level responsibility until the RPCs themselves branch on the role.
- DOC-11 | fixed (doc) | §7.10 + §9: both badge thresholds are app constants (`FOOD_COST_WARN 30` / `FOOD_COST_DANGER 35` in `src/lib/format.ts`); the DB stores only `food_cost_target_pct` (35, validated 0–100) and **no SQL reads it**; there is deliberately no `food_cost_warn_pct` key. Adding one would be a settings/UX feature, not a contract fix — documented instead.
- DOC-12 | fixed (doc) | §5.9: opex is accrual by `expense_date` and includes `pending` (an unpaid bill is already a cost); the cash view is `status='paid'` + `get_dashboard_stats().pending_expenses_amount`. Labor has the same accrual shape (period of any status; `advance_deduction` is the cash side).
- DOC-13 | fixed (doc) | §3 `supplier_payment_allocations` row: the table is not in the spec's list and is how "payment updates purchase_orders" is implemented — the app must read the allocations, never `supplier_payments.purchase_order_id` (null for a FIFO/trừ dần payment), to know which POs a payment settled. §7.1/§7.2 already read it for payment history.
